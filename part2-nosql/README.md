# Part 2 — NoSQL (MongoDB)

## Files
- products_catalog.json: Provided catalog dataset
- nosql_analysis.md: Theory justification (RDBMS limitations, MongoDB benefits, trade-offs)
- mongodb_operations.js: Load + queries + update + aggregations

## Run MongoDB (Docker)
From repo root:
```bash
docker compose up -d
docker ps
